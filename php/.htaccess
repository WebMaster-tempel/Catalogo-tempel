Options -Indexes
RewriteEngine On

# Serve real files and directories as-is
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# API requests go to the PHP router
RewriteRule ^api/ - [L]

# Everything else → React SPA
RewriteRule ^ index.html [L]
